# 今晚 30 分钟上线清单

> ✅ **本站已上线：https://solve-lab.com/**
> 仓库：https://github.com/zxdxs/problem-solving-coach

> 核心判断：**上线不需要手机号、不需要微信、不需要域名、不需要备案。**
> 这四样都是"以后优化"的事，不是"今晚阻塞"的事。

---

## 零、先算一笔账：今晚的真实成本

| 项目 | 是否必须 | 成本 |
|---|---|---|
| 托管（Vercel / Netlify） | 必须 | **0 元** |
| 域名 | ❌ 不必须 | **0 元**（先用免费二级域名） |
| 手机号 / 微信 | ❌ 不必须（那是互动用的） | 0 元 |
| 备案 | ❌ 不必须（用海外节点） | 0 元 |
| **今晚合计** | | **0 元** |

结论：**今晚就能挂上去，而且一分钱不花。**
等有第一笔稿费了，再买自己的域名（.com 约 60–100 元/年），那时候是"花赚来的钱"，不是"掏自己的钱"。

---

## 一、三条路径，选一条（推荐第一条）

| 方式 | 耗时 | 需要什么 | 国内访问 | 推荐度 |
|---|---|---|---|---|
| **A. Vercel 拖拽** | 5 分钟 | 邮箱注册 | 一般，部分地区不稳 | ⭐ **今晚首选** |
| B. Netlify 拖拽 | 5 分钟 | 邮箱注册 | 一般 | 备选 |
| C. GitHub Pages | 15 分钟 | GitHub 账号 | 一般 | 适合后续共建 |

> **先跑通，别追求完美。** 今晚的目标只有一个：让网站在互联网上有个能访问的网址。
> 访问速度、自定义域名、国内节点——全是以后的事。

---

## 二、Vercel 上线（推荐，5 分钟）

### 步骤

1. 打开 **https://vercel.com/new**
2. 用 **GitHub 账号登录**（没有就用邮箱注册一个，不需要手机号）
3. 登录后，直接把 `problem-solving-coach` **整个文件夹拖进页面中间的上传区**
4. 等它自动部署（约 30–60 秒）
5. 完成后会给你一个网址，形如：
   ```
   https://problem-solving-coach-xxxx.vercel.app
   ```
6. 点开，检查首页、执行单、自测三个页面是否正常

✅ **完成。网站已经上线了。**

### 以后怎么更新

- 改了本地文件 → 重新拖一次文件夹 → 新版本自动上线
- 或者把文件夹推到 GitHub，Vercel 会自动同步（更适合长期维护）

---

## 三、部署前，花 5 分钟改一处配置

打开 `assets/data.js`，最顶部的 `PSC_SITE`，改成你自己的：

```js
const PSC_SITE = {
  name: '解决问题训练站',
  email: '你的邮箱@example.com',        // ← 改这里
  github: 'https://github.com/你的用户名/仓库名',  // ← 改这里（还没建就先留空字符串）
  community: '',                        // 明天拿到微信号后填，例如：'微信群：加微信 xxx 备注「解题」'
  maintainer: '你的署名'                 // ← 改这里
};
```

改完**重新拖一次文件夹**即可更新。

> 未配置时联系页会显示占位提示，**不会挂假链接误导访客**——所以今晚可以先留空，明天再补。

---

## 四、上线后自检（5 分钟）

打开你的网址，逐项确认：

- [ ] 首页能打开，8 个章节卡片都在
- [ ] 进入「五步执行单」，填个题目 → 点「保存」→ 切到「我的练习库」能看到记录
- [ ] 进入「掌握度自测」，随便答几题 → 点「提交并判分」→ 出现 L1–L4 等级
- [ ] 导航栏右上角「掌握度 X%」会随勾选变化
- [ ] 手机浏览器打开（微信里打开也试试），排版没乱
- [ ] 点「联系与共建」页，复制网址按钮能用

**全部打勾 = 上线成功。**

---

## 五、明天做什么（办完手机号之后）

| 顺序 | 动作 | 说明 |
|---|---|---|
| 1 | 开新微信 | 专门用于这个站的互动，和个人号分开 |
| 2 | 建微信群 | 在 `contact.html` 的 `PSC_SITE.community` 里填上群信息或二维码说明 |
| 3 | 重新拖一次文件夹 | 更新网站 |
| 4 | 把网址发到 1–2 个相关社群 | 先小范围测试反馈，别一上来就铺开 |

---

## 六、以后再优化的事（现在别管）

以下全是"有收入之后"才需要考虑的，**现在管了就是精力陷阱**：

| 事项 | 什么时候做 |
|---|---|
| 买自己的域名 | 拿到第一笔稿费后（约 60–100 元/年） |
| 国内访问加速 | 有稳定访客后（考虑腾讯云 COS + CDN，需备案） |
| 备案 | 决定用大陆服务器时（1–20 个工作日） |
| 账号系统 / 评论 | 确认真的有人需要时（接 Supabase，免费额度够用） |

---

## 七、一条重要的心理预期

绝大多数个人站点最终都会沉寂——这是常态，不是你的失败。

所以上线之后，**把成功的定义换掉**：

| ❌ 别看这个 | ✅ 看这个 |
|---|---|
| 页面浏览量 | 有没人**完成了一份五步执行单** |
| 访客数 | 有没人**通过自测达到 L3** |
| 收藏数 | 有没人**加群、发来一封认真的反馈** |
| — | 有没人**提交了第一个 PR**（哪怕只改一个错别字） |

> **哪怕只有一个人，因为你的内容，用五步策略解决了他真实工作中的一个难题——这件事的价值，远大于十万个点开就关的浏览量。**

---

## 八、今晚的一句话总结

> **先让它在互联网上存在，再让它变好。**
> 一个 60 分的网站上线了，胜过 100 分的网站躺在硬盘里。
