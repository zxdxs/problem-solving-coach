# 腾讯云 COS 部署清单

> **对象**：已通过实名认证的腾讯云账号
> **目标**：把静态网站放到成都节点，国内秒开
> **预计耗时**：20 分钟
> **费用**：正常访问约几毛～几元/月（50GB 免费额度内）

---

## 零、先说结论

| 项目 | 值 |
|---|---|
| 地域选择 | **成都 `ap-chengdu`**（若被卡备案，改用香港 `ap-hongkong`） |
| 访问权限 | **公有读私有写** |
| 静态网站首页 | `index.html` |
| 是否需要备案 | 用 COS 默认域名通常**免备案**；用自己的域名**必须备案** |

---

## 一、创建存储桶

您已经走到确认页了，信息是：

```
名称/地域：kesunweng-1485151958（公有云园区-中国-成都）
请求域名：kesunweng-1485151958.cos.ap-chengdu.myqcloud.com
```

**✅ 直接点「确定」。**

> ⚠️ 弹窗提示"名称与地域创建后无法修改"——这没关系，成都就是您要的。

### 如果之后发现成都需要备案

删掉这个桶，重建一个地域选 **香港 `ap-hongkong`** 的。香港确定免备案，对成都用户也比美国快得多。

---

## 二、改为「公有读」

创建后默认是私有写。要开静态网站必须先改公有读：

1. 进桶 → 「**权限管理**」→「**存储桶访问权限**」
2. 改为 **公有读私有写**
3. 弹窗出现后：
   - ☑️ **勾选「同意协议」**（不勾的话"仍要修改"是灰的，点不了）
   - 点蓝色「**仍要修改**」

### ⚠️ 改完立刻做：设置账单告警（最重要）

腾讯云控制台 → 「**费用中心**」→ 「预算管理」或「告警设置」

- 阈值设 **10 元**
- 超过就短信通知

> 这是防止被盗刷的唯一有效手段。**2 分钟，必做。**

### 推荐再做：配置防盗链

COS 控制台 → 「**安全管理**」→「**防盗链设置**」

- 开启 **Referer 白名单**
- 填上您的访问域名（先用 COS 默认域名）

效果：别人盗链您的图片会显示不出来，省下大量流量费。

---

## 三、开启静态网站

1. 「**基础配置**」→「**静态网站**」
2. 打开开关
3. 配置：
   - **首页**：`index.html`
   - **错误页**：留空（或填 `index.html`）
4. 保存

保存后页面会显示**访问地址**，形如：

```
https://kesunweng-1485151958.cos-website.ap-chengdu.myqcloud.com
```

> ⚠️ 注意域名里有 **`-website`**，和普通请求域名 `cos.ap-chengdu...` 不一样。
> **静态网站用的是带 `-website` 的那个。**

---

## 四、上传文件

1. 「**文件列表**」→「**上传文件**」（或拖拽）
2. 把解压后的 **全部内容** 传上去：
   - 15 个 `.html`
   - 整个 `assets/` 文件夹（含 `wechat-qr.jpg`）
   - `README.md`、`DEPLOY.md`、`LICENSE.txt`（可选）

> ⚠️ **注意层级**：上传后根目录应该直接是 `index.html`，
> 不能是 `problem-solving-coach/index.html`（多一层就打不开）。

---

## 五、验证

打开您的 `cos-website` 网址，检查：

- [ ] 首页能打开（深蓝色 + 「5 步 / 4 层 / 8 章 / 0 元」）
- [ ] 导航栏有 **「贡献者」**
- [ ] 「联系与共建」页有**微信二维码**（课孙翁）
- [ ] 「贡献者」页显示 **课孙翁**
- [ ] **手机微信里打开一次**（排版最容易在这出问题）
- [ ] 「五步执行单」填题 → 保存 → 练习库能看到

---

## 六、以后怎么更新文件

改了本地文件后：

1. 进 COS 控制台 → 文件列表
2. 同名文件**直接覆盖上传**即可
3. 刷新网页（可能需要 Ctrl+F5 强刷）

> COS 没有"部署"这个概念，传上去即时生效，比 GitHub Pages 还快。

---

## 七、常见问题

### Q：访问显示 404 / 打不开
- 检查文件路径：根目录是否直接是 `index.html`
- 检查静态网站开关是否打开、首页是否填了 `index.html`

### Q：显示"需要备案"
- 换**香港**地域重建桶（确定免备案）

### Q：图片显示不出来
- 检查是否公有读
- 检查 `assets/` 文件夹是否完整上传

### Q：被刷流量了怎么办
- 立刻去控制台**关闭静态网站**或**改回私有读**
- 账单告警就是为这一刻准备的

---

## 八、费用参考

| 场景 | 费用 |
|---|---|
| 1000 次访问（约 300MB） | 约 0.15 元 |
| 1 万次访问（约 3GB） | 约 1.5 元 |
| 被恶意刷 1TB | 约 500 元 ⚠️ |

**正常个人站点，一个月几毛到几块钱。** 真正的风险只有盗刷——所以账单告警必做。

---

## 九、下一步

网站跑起来之后：

1. 买自己的域名（约 60–100 元/年）→ 绑定 → **但需备案**
2. 配 Cloudflare Email Routing（免费）→ 用域名邮箱
3. 写投稿文章 → 稿费 → 正向循环

**现在先把网站跑起来。**
